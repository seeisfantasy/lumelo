# Writable runtime configuration.
# Development images can seed this from the default config on first boot.

mode = "local"
interface_mode = "ethernet"
dsd_output_policy = "native_dsd"
ssh_enabled = false
ui_theme = "system"
